# 로켓단 폭탄 돌리기 — PRD

**버전** 1.1 · **작성일** 2026-09-20 · **상태** M1~M9 구현·검증 완료 (M10 실사용 테스트 남음)

---

## 1. 한 줄 요약

아이패드를 식탁 가운데 두고 4명(아이·어른 섞임)이 대각선으로 둘러앉아, 자기 차례에 뜨는 숫자 퀴즈를 풀어 옆으로 폭탄을 넘기는 게임. 시간이 다 되거나 두 번 틀리면 그 자리에서 폭탄이 터진다.

---

## 2. 왜 만드는가

- 온 가족이 **패드 하나로** 같이 노는 놀이가 필요하다. 각자 폰을 보는 게 아니라 한 화면을 둘러싸고 논다.
- 7~8살 아이와 어른이 **같은 판에서 대등하게** 겨룰 수 있어야 한다. 난이도를 사람마다 다르게 주면 가능하다.
- 아이가 지더라도 **바로 다음 판**으로 갈 수 있어야 한다. 누적 점수나 순위는 상처를 남긴다.

### 성공 기준

| 항목 | 기준 |
|---|---|
| 첫 판 시작까지 | 설정 화면에서 **40초 이내** (4명 등록 포함) |
| 한 판 길이 | 60~120초 |
| 아이 승률 | 어른 상대로 **40% 이상** (난이도 분리가 제대로 작동한다는 뜻) |
| 아이 혼자 조작 | 어른 도움 없이 자기 차례를 끝낼 수 있다 |
| 네트워크 | **불필요** — 비행기 모드에서도 전부 동작 |

### 이번 범위가 **아닌** 것

- 여러 기기 멀티플레이 (한 패드에서만)
- 계정·로그인·클라우드 저장
- 누적 랭킹, 시즌, 업적
- PokéAPI 연동 (아래 3.3 참고)
- 안드로이드 네이티브 앱

---

## 3. 핵심 결정과 그 이유

인터뷰에서 확정된 내용. 구현 중 흔들리면 이 표로 돌아온다.

### 3.1 확정 목록

| # | 결정 | 이유 |
|---|---|---|
| D1 | 화면 4분할, **내 차례에만 내 구역이 열림** | 폭탄이 "돌아가는" 느낌이 가장 강하다. 대기 중인 세 구역은 어둡게 두어 시선이 자기 차례로 모인다. |
| D2 | 퀴즈가 **차례인 사람 쪽으로 0/90/180/270° 회전** | 가운데 둔 패드에 대각선으로 앉으면 글자 방향이 사람마다 다르다. 푸는 사람만 바로 읽으면 된다. |
| D3 | **순수 난이도 단계만 분리** (소재 장식 없음) | 아이는 글을 읽는 데 시간을 뺏기면 안 된다. 숫자만 보고 바로 푼다. |
| D4 | 어른 목숨 0 → **즉시 폭발, 그 사람 패배** | 규칙이 짧아 설명하기 쉽다. 아이는 매 턴 초기화라 확실히 유리하다. |
| D5 | **PokéAPI 안 씀 — 완전 오프라인** | 네트워크 실패 지점이 사라진다. 로켓단 캐릭터는 자체 에셋. |
| D6 | 어른 난이도 = **두 자리 곱셈 / 나머지** | 암산으로 10초 안에 가능하지만 긴장된다. 보기를 근사값으로 깔면 찍기 어렵다. |
| D7 | **총시간 숨김**, 소리로만 느낌 전달 | 언제 터질지 몰라야 서두른다. 남은 초를 보여주면 긴장이 사라진다. |
| D8 | 폭발 후 **바로 다음 판 시작 버튼** | 누적 점수·순위 없음. 아이가 져도 바로 다음 판으로 넘어간다. |
| D9 | 진동 포기, **소리 + 화면 흔들기**로 대체 | iOS 사파리에 `navigator.vibrate`가 없다. 대안이 아니라 원래 설계로 삼는다. |
| D10 | **웹 URL 배포** (정적 호스팅) | 파일 전달 없이 링크만 열면 된다. |
| D11 | 이미지 에셋 **6장** (캐릭터 4 + 폭탄 + 폭발) | 나머지 UI는 CSS로. Codex로 한 번에 생성 가능한 분량. |

### 3.2 캐릭터

로켓단 4인 — **로이 · 로사 · 냐옹 · 마자용**.

역할은 **자리 표식**이다. 게임 능력 차이는 없다. 참가자가 설정에서 캐릭터를 골라 자기 자리에 앉힌다.

> **저작권 주의**: 포켓몬 공식 아트를 그대로 쓰지 않는다. Codex로 **원작을 연상시키되 직접 그린** 캐릭터를 생성한다 (§8). 가정 내 개인 사용 범위를 벗어나 공개 배포할 경우 이름도 자체 명칭으로 바꾼다.

### 3.3 PokéAPI를 쓰지 않기로 한 이유

애초 구상은 PokéAPI였으나 인터뷰 중 제외됐다. 기록해 둔다:

- 로켓단 캐릭터는 **PokéAPI에 없다**. API는 포켓몬 데이터(이름/타입/능력치/스프라이트)만 제공한다.
- 퀴즈를 순수 숫자로 가기로 하면서 API 데이터를 쓸 자리가 사라졌다.
- 네트워크 의존이 없어져 **비행기 모드·지하·차 안**에서도 동작한다.

나중에 "잡은 포켓몬 수집" 같은 보상을 붙이고 싶어지면 그때 다시 연다. 그 경우에도 게임 진행은 오프라인으로 유지하고, 보상만 실패해도 되는 선택적 레이어로 붙인다.

---

## 4. 게임 규칙

### 4.1 판의 흐름

```
[설정 화면]  4명 등록 (이름·유형·캐릭터·자리)
     ↓
[준비 화면]  "폭탄을 넘기세요" — 시작 버튼
     ↓
┌──> [턴 시작]  현재 자리 구역이 밝아짐, 퀴즈 등장 (해당 방향으로 회전)
│         ↓
│    [답 선택]  보기 4개 중 하나 탭
│         ↓
│    정답 → 다음 사람으로 ─────┘
│    오답 1회 → 경고 연출, 같은 자리에서 새 문제
│    오답 2회 → 폭발
     ↓
[폭발]  소리 + 화면 흔들기 + 붉은 플래시 + 폭발 이미지
     ↓
[결과]  "○○ 폭발!" + [다시 하기] 큰 버튼
```

타이머는 **판 전체**에 걸린다. 턴마다 초기화되지 않는다. 그래서 앞사람이 오래 끌면 뒷사람이 불리하다 — 이게 폭탄 돌리기의 핵심 긴장이다.

### 4.2 목숨 규칙

| 유형 | 목숨 | 소진 조건 | 회복 |
|---|---|---|---|
| **아이** | 매 턴 2개 | 그 턴에 2번 틀림 → 폭발 | **매 턴 시작 시 2개로 초기화** |
| **어른** | 판 전체 2개 | 판 내 누적 2번 틀림 → 폭발 | 없음 (판이 끝나야 회복) |

핵심: 아이는 한 턴에 두 번 틀려야 지지만, 어른은 **서로 다른 턴에서 한 번씩만 틀려도** 진다.

UI에는 각 자리 구역에 하트 아이콘으로 표시한다. 아이 자리는 항상 ♥♥, 어른 자리는 소진된 만큼 회색.

### 4.3 폭발 조건

세 가지 중 하나라도 걸리면 그 자리에서 폭발:

1. **타이머 만료** — 판 총시간이 다 됨. 그 순간 차례인 사람이 패배.
2. **아이가 그 턴에 2번 틀림**
3. **어른의 누적 목숨이 0이 됨** (2번째 오답 즉시)

### 4.4 타이머

- 판 총시간: **60~120초 사이 무작위** (매 판 다르게 뽑음)
- 화면에 숫자 **표시하지 않음**
- 대신 **똑딱 소리**가 시간 경과에 따라 빨라진다:

| 남은 시간 비율 | 똑딱 간격 | 추가 연출 |
|---|---|---|
| 100~50% | 1000ms | — |
| 50~25% | 600ms | — |
| 25~10% | 350ms | 화면 가장자리 옅은 붉은 맥동 |
| 10~0% | 180ms | 맥동 강해짐 |

남은 초를 못 봐도 **소리 속도로 체감**된다. 아이도 "빨라졌다 = 곧 터진다"를 금방 배운다.

---

## 5. 퀴즈 명세

### 5.1 아이 난이도 (7~8살)

| 유형 | 형식 | 범위 | 비중 |
|---|---|---|---|
| 덧셈 | `a + b` | a,b ∈ 1~9, 합 ≤ 18 | 40% |
| 뺄셈 | `a - b` | a ∈ 2~18, b < a, 결과 ≥ 1 | 40% |
| 10 만들기 | `7 + ? = 10` | 합 = 10 | 20% |

- 정답은 항상 **한 자리 또는 10 이하**
- 오답 보기: 정답 ±1, ±2, ±3 중 3개 무작위 (음수·0 제외)
- 목표 풀이 시간: **3~5초**

### 5.2 어른 난이도

| 유형 | 형식 | 범위 | 비중 |
|---|---|---|---|
| 두 자리 곱셈 | `a × b` | a ∈ 12~29, b ∈ 12~29 | 50% |
| 나머지 | `a ÷ b`의 나머지 | a ∈ 100~999, b ∈ 7~19 | 30% |
| 혼합 연산 | `a × b - c` | a,b ∈ 6~19, c ∈ 10~99 | 20% |

- 오답 보기는 **근사값**으로 깐다 — 찍기 방지의 핵심
  - 곱셈: 정답 ±(b), ±(a), ±10 등 **실수하기 쉬운 값** (예: 23×17=391 → 381, 401, 371)
  - 나머지: 정답 ±1, ±2, 그리고 `b - 정답`
- 목표 풀이 시간: **8~12초**

### 5.3 보기 배치

- 항상 **4개**, 2×2 그리드
- 정답 위치 무작위
- 각 버튼 최소 **88×88px** (44px 기준의 2배 — 회전된 상태에서 누르므로 여유를 둔다)
- 버튼 간 간격 최소 16px (오답 방지)

### 5.4 문제 중복 방지

한 판 안에서 같은 문제(a, b, 연산자가 모두 동일)는 다시 나오지 않는다. 판이 끝나면 초기화.

---

## 6. 화면 설계

### 6.1 레이아웃 (아이패드 가로, 1180×820 기준)

```
┌───────────────────────┬───────────────────────┐
│  자리 A (180° 회전)    │  자리 B (270° 회전)    │
│  ⬛ 어두움 · 대기      │  ⬛ 어두움 · 대기      │
│  [캐릭터] 이름 ♥♥     │  [캐릭터] 이름 ♥♡     │
├───────────────────────┼───────────────────────┤
│  자리 C (90° 회전)     │  자리 D (0° 회전)      │
│  ⬛ 어두움 · 대기      │  ⬜ 밝음 · 내 차례!    │
│  [캐릭터] 이름 ♥♥     │  ┌─────────────────┐  │
│                       │  │   23 × 17 = ?   │  │
│                       │  │ ①381 ②391      │  │
│                       │  │ ③401 ④371      │  │
│                       │  └─────────────────┘  │
└───────────────────────┴───────────────────────┘
              💣 (중앙, 현재 자리 쪽으로 기울어짐)
```

- **중앙 폭탄**: 네 구역 경계에 겹쳐 놓는다. 현재 차례인 구역 쪽으로 살짝 기울고, 심지 불꽃이 똑딱 속도에 맞춰 깜빡인다.
- **회전**: `transform: rotate()`. 자리 A=180°, B=270°, C=90°, D=0° (D가 정방향 기준 = 패드 아래쪽에 앉은 사람).
- **활성 구역**: 밝기 100%, 나머지는 `filter: brightness(0.35)`.

### 6.2 화면 목록

| 화면 | 내용 |
|---|---|
| **S1 설정** | 참가자 4명 등록. 각자: 이름(선택, 비우면 캐릭터명), 유형(아이/어른 토글), 캐릭터(4종 중 택1, 중복 불가), 자리(A~D). |
| **S2 준비** | "폭탄이 돌기 시작합니다" + 시작 순서 표시 + 큰 [시작] 버튼 |
| **S3 플레이** | §6.1 레이아웃 |
| **S4 폭발** | 전체 붉은 플래시 → 폭발 이미지 → shake → "○○ 폭발!" |
| **S5 결과** | 패배자 캐릭터 + [다시 하기] (같은 멤버) + [멤버 바꾸기] (S1로) |

### 6.3 연출 상세

**오답 1회 (경고)**
- 선택한 버튼 붉게 흔들림 (0.4초)
- 짧은 경고음 (낮은 삑)
- 남은 하트 하나가 회색으로 (어른만)
- 0.6초 뒤 같은 자리에서 **새 문제**

**정답**
- 선택 버튼 초록 플래시 (0.3초)
- 경쾌한 딩
- 폭탄이 다음 자리로 미끄러지는 애니메이션 (0.5초)
- 새 구역이 밝아짐

**폭발**
- 화면 전체 흰색 → 붉은색 플래시 (0.15초)
- 폭발 이미지 중앙에 확대 등장
- `body` 전체 shake — `translate` + `rotate` 무작위, 진폭 12px, 0.8초
- 폭발음 (§7.3)
- 1.2초 뒤 S5로

> **shake 구현 주의**: `body`에 `transform`을 걸면 내부 `position: fixed`가 깨진다. shake 전용 래퍼 `<div id="shake-root">`를 두고 그 안에 화면을 넣는다.

### 6.4 접근성·안전

- 폭발 플래시는 **3회 이하**, 초당 3회를 넘지 않는다 (광과민성 발작 예방)
- 설정에 **[플래시 끄기]** 토글 — 켜면 붉은 플래시 대신 부드러운 페이드
- 설정에 **[소리 끄기]** 토글 — 끄면 똑딱 대신 폭탄 심지 시각 애니메이션이 강해진다
- 색으로만 정보를 전달하지 않는다 (오답=붉은색 + 흔들림, 정답=초록 + 체크 표시)

### 6.5 표시 위치 규칙

전역 규칙에 따라: **하트·경고 표시를 문제 텍스트 위에 겹치지 않는다.** 각 구역의 **상단 모서리**(회전 기준)에 배치하고 크기는 구역 높이의 `8%`로 잡는다.

---

## 7. 기술 스펙

### 7.1 스택

| 항목 | 선택 | 이유 |
|---|---|---|
| 형태 | 정적 웹앱, **바닐라 JS** | 프레임워크 불필요한 규모. 빌드 단계 없음. |
| 파일 구조 | 폴더 분리 (html/css/js/assets) | 이미지 교체가 잦을 것. URL 배포라 단일 파일 이점 없음. |
| 배포 | 정적 호스팅 (URL 접속) | D10 |
| 오프라인 | **Service Worker로 전체 캐시** | URL 배포지만 한 번 열면 오프라인 동작 |
| 대상 | iPadOS Safari 16+ (주), Chrome/Edge (보조) | |
| 상태 저장 | `localStorage` — 마지막 멤버 구성만 | 재방문 시 설정 시간 단축. 없어도 동작해야 함. |

```
/
├─ index.html
├─ sw.js                    Service Worker
├─ manifest.webmanifest     홈화면 추가용
├─ css/
│   ├─ lock.css             ← pad-input-lock 스킬
│   └─ game.css
├─ js/
│   ├─ lock.js              ← pad-input-lock 스킬 (가장 먼저 로드)
│   ├─ quiz.js              문제 생성기
│   ├─ audio.js             WebAudio 사운드
│   ├─ game.js              상태 기계
│   └─ ui.js                렌더링
└─ assets/
    ├─ char-roy.png  char-rosa.png  char-meowth.png  char-wobbuffet.png
    ├─ bomb.png
    └─ explosion.png
```

### 7.2 패드 입력 잠금 (필수)

전역 규칙 및 `pad-input-lock` 스킬에 따라 **처음부터** 적용한다. 나중에 붙이면 화면 전체를 다시 훑어야 한다.

세 겹 전부:

1. **viewport meta**
   ```html
   <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover, maximum-scale=1, user-scalable=no">
   ```
2. **`css/lock.css`** — 스타일시트 맨 위. `html, body` 규칙과 `input, textarea` 예외 규칙을 **한 쌍으로** 넣는다 (설정 화면의 이름 입력칸이 살아 있어야 한다).
3. **`js/lock.js`** — `<head>`에서 **다른 모든 스크립트보다 먼저** 로드.

**함정 회피** (스킬에 기록된 것):
- `touchend`를 막아 더블탭 확대를 해결하지 **않는다** — 아이가 버튼을 빠르게 두 번 누르는 것까지 죽는다. `touch-action`으로 충분하다.
- `touch-action: none`이나 `pan-y`로 쓰지 **않는다** — `pan-x pan-y`를 쓴다.

**검증** (구현 완료 조건에 포함):
```bash
PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python scripts/verify_lock.py index.html
```
"CSS 넣었으니 됐다"로 넘기지 않는다. 이 영역은 눈으로 안 보인다.

### 7.3 사운드 (WebAudio)

파일 없이 **WebAudio API로 합성**한다. 에셋 크기가 줄고 지연이 없다.

| 소리 | 합성 방법 |
|---|---|
| 똑딱 | 짧은 square 파형 40ms, 800Hz, 급감쇠 |
| 정답 | sine 2음 상행 (C5→E5), 각 80ms |
| 경고 | sawtooth 200Hz, 250ms, 약한 왜곡 |
| 폭발 | white noise 버스트 + 저역 통과 필터 스윕(2000→80Hz), 1.2초 |

> **iOS 필수**: WebAudio는 **사용자 제스처 안에서** `AudioContext`를 생성/resume 해야 소리가 난다. S2 준비 화면의 [시작] 버튼 `click` 핸들러에서 `ctx.resume()`을 호출하고, 무음 버퍼를 한 번 재생해 언락한다. 이걸 빠뜨리면 **첫 판이 통째로 무음**이 된다.

### 7.4 진동

`navigator.vibrate`는 iPadOS Safari에 **없다**. 코드에 넣지 않는다 (D9). 폭발 피드백은 소리 + shake로 완결한다.

### 7.5 상태 모델

```js
const state = {
  screen: 'setup' | 'ready' | 'play' | 'boom' | 'result',
  players: [
    { id, name, type: 'kid'|'adult', character, seat: 'A'|'B'|'C'|'D',
      livesLeft: 2, wrongThisTurn: 0 }
  ],
  turnIndex: 0,          // players 배열 인덱스
  roundEndsAt: 0,        // performance.now() 기준 절대 시각
  roundTotalMs: 0,       // 60000~120000 무작위
  usedQuestions: Set,    // 중복 방지 키
  currentQuestion: { text, choices: [4], answerIndex },
  loserId: null,
  settings: { sound: true, flash: true }
};
```

**타이머는 `setInterval` 카운트다운이 아니라 `roundEndsAt` 절대 시각 기준**으로 구현한다. 아이패드를 잠그거나 앱을 전환하면 타이머가 멈추는데, 절대 시각 기준이면 돌아왔을 때 정확하다.

`visibilitychange`로 화면이 숨겨지면 **판을 일시정지**하고 (`roundEndsAt`을 남은 시간만큼 뒤로 밀기) 돌아오면 "계속하기" 버튼을 띄운다. 그래야 실수로 홈 버튼을 눌러 터지는 일이 없다.

### 7.6 성능

- 폭발 shake는 `transform`만 쓴다 (`top`/`left` 금지) — 리페인트 회피
- 똑딱 소리는 `setTimeout` 재귀가 아니라 `AudioContext.currentTime` 스케줄링 (드리프트 방지)
- 이미지는 `<img>` 프리로드 후 게임 시작 — 폭발 순간 이미지가 늦게 뜨면 연출이 죽는다

---

## 8. 이미지 에셋 — Codex 생성 명세

총 **6장**. Codex(사용자 계정)로 생성한다.

### 공통 지시

```
스타일: 90년대 애니메이션 셀 채색 느낌, 굵고 또렷한 검은 외곽선,
       평면 채색(그라데이션 최소), 채도 높은 색
배경: 완전 투명 (PNG, alpha)
크기: 1024×1024
구도: 캐릭터 전신 또는 상반신, 중앙 배치, 가장자리 여백 10%
주의: 기존 애니메이션의 공식 아트를 복제하지 말 것. 원작을 연상시키되
     독자적으로 그린 캐릭터여야 함.
```

### 개별 프롬프트

| 파일 | 프롬프트 |
|---|---|
| `char-roy.png` | 위 공통 지시 + "악당 조직의 남성 단원. 어깨까지 오는 짙은 남색 머리, 흰색 제복 상의에 가슴 한가운데 큰 빨간 R 글자, 검은 장갑과 부츠. 한쪽 손을 허리에 얹고 자신만만하게 웃는 상반신." |
| `char-rosa.png` | 위 공통 지시 + "악당 조직의 여성 단원. 길고 풍성한 진홍색 머리, 흰색 크롭 제복에 가슴 한가운데 큰 빨간 R 글자, 검은 긴 장갑과 부츠. 팔짱을 끼고 도도하게 웃는 상반신." |
| `char-meowth.png` | 위 공통 지시 + "말하는 고양이형 마스코트 캐릭터. 크림색 털, 이마 한가운데 둥근 금색 부적, 뾰족한 귀, 갈색 발끝, 능글맞게 웃으며 앞발을 들어 흔드는 전신." |
| `char-wobbuffet.png` | 위 공통 지시 + "파란색 둥근 몸통의 마스코트 생물. 커다란 검은 눈과 크게 벌린 입, 짧은 팔 두 개를 위로 들고 경례하듯 서 있는 전신, 아래쪽에 검은 꼬리." |
| `bomb.png` | 위 공통 지시 + "고전 만화풍 구형 폭탄. 새까만 광택 있는 구, 위에 짧은 도화선, 도화선 끝에 주황색 불꽃과 작은 불똥. 구 표면에 흰색 하이라이트 한 점. 정면." |
| `explosion.png` | 위 공통 지시 + "만화풍 폭발 구름. 주황-노랑-흰색 세 겹의 뾰족한 별 모양 폭발, 굵은 검은 외곽선, 바깥으로 튀는 파편 조각들. 정면, 좌우 대칭." |

### 생성 후 처리

1. 배경 투명 확인 (아니면 제거)
2. **512×512로 리사이즈** — 1024는 과하다. 패드 구역 하나가 약 590×410px.
3. PNG 최적화 (`oxipng` 또는 TinyPNG) — 각 장 **80KB 이하** 목표
4. 4개 캐릭터의 **시각적 무게가 비슷한지** 눈으로 확인. 한 명만 유독 크거나 어두우면 다시 생성.

---

## 9. 구현 순서

각 단계 끝에서 아이패드(또는 브라우저 기기 에뮬레이션)로 실제 확인한다.

| 단계 | 내용 | 완료 조건 |
|---|---|---|
| **M1** | 프로젝트 골격 + `pad-input-lock` 3겹 적용 | ✅ `verify_lock.py` 8항목 전부 통과, 입력칸 예외 살아 있음 |
| **M2** | 퀴즈 생성기 (`quiz.js`) 단독 | ✅ 각 300문제 — 수식·보기 중복·음수·물음표 검증 통과, 6유형 전부 등장 |
| **M3** | 4분할 회전 레이아웃 | ✅ 4방향 좌표 확인 (head→text→choices 순서 정확), 버튼 88px, overflow 없음 |
| **M4** | 상태 기계 + 턴 진행 | ✅ 목숨 규칙 7항목 검증 (아이 턴별 초기화 / 어른 누적) |
| **M5** | 타이머 + WebAudio 똑딱 | ✅ 템포 4단계 전환, 일시정지 중 시간 고정, 타이머 만료 폭발 |
| **M6** | 폭발 연출 | ✅ 플래시·shake·폭발 이미지 동작, `position: fixed` 유지 |
| **M7** | Codex 이미지 6장 | ✅ 512px·합계 95KB 로 최적화, 프리로드 적용 |
| **M8** | 설정 화면 + localStorage + 접근성 토글 | ✅ 4자리 카드, 캐릭터 중복 시 자동 교환, 토글 3종 |
| **M9** | Service Worker + 배포 | ✅ 16개 파일 캐시, 오프라인 재방문 시 게임 완주 확인 |
| **M10** | 실사용 테스트 (아이 포함 4인 3판) | ⬜ **남음** — 실제 아이패드에서 |

---

## 9-2. 구현하며 바뀐 것

| 항목 | PRD 초안 | 실제 | 이유 |
|---|---|---|---|
| 자리 회전 | A=180 B=270 C=90 D=0 | **A=180 B=180 C=0 D=0** (기본) | 90°/270° 는 가로세로가 뒤바뀌어 글자가 작아진다. 마주보고 둘씩 앉는 배치를 기본으로 하고, 네 방향 배치는 `around` 모드로 남겨뒀다 (`__ui.setSeatMode('around')`) |
| 캐릭터 크기 | 균일하다고 가정 | **개별 `scale` 보정** | 그림마다 인물 비율(전신/상반신)이 달라 한 명만 작아 보였다. 여백 문제가 아니라 크롭으로는 못 고친다 |
| 대기 구역 캐릭터 | 회전 | **그림만 정방향** | 물구나무선 캐릭터는 아이에게 어색하다. 이름·하트만 그 사람 쪽으로 돌린다 |
| 문제 텍스트 | UI 가 `= ?` 를 붙임 | **quiz.js 가 완결된 문장을 냄** | `6 + ? = 10` 유형에서 `= ? = ?` 가 됐다 |
| 대기 중 하트 | 활성 구역만 | **대기 구역에도 표시** | 어른 목숨이 몇 개 남았는지는 자기 차례가 아니어도 보여야 긴장이 산다 |
| 시작 순서 | 고정 | **매 판 무작위** | 늘 같은 사람이 먼저 시작하면 불공평하다 |

## 10. 열린 위험

| 위험 | 영향 | 대응 |
|---|---|---|
| 어른 난이도가 실제로는 너무 어려워 판이 안 넘어감 | 게임이 지루해짐 | M10에서 측정. 어른 평균 풀이 시간이 15초를 넘으면 곱셈 범위를 12~19로 축소 |
| 아이가 계속 이겨서 어른이 재미없어함 | 어른 참여 저조 | 설정에 어른 난이도 3단계(쉬움/보통/어려움) 추가 — M8 이후 |
| 회전된 화면에서 아이가 방향을 헷갈림 | 조작 실패 | 활성 구역에 방향 화살표 또는 "○○ 차례!" 를 크게. M3에서 확인 |
| 4명이 패드 하나를 동시에 만짐 | 남의 차례에 오답 입력됨 | 비활성 구역은 `pointer-events: none` — 물리적으로 못 누름 |
| 폭발 연출이 아이에게 너무 무서움 | 놀이 중단 | 설정 [순한 폭발] 토글 — 플래시·소리 강도를 절반으로. M8에서 추가 검토 |

---

## 부록 A. 규칙 요약 카드 (화면에 표시할 문구)

> 🎯 **폭탄 돌리기**
> 1. 내 차례에 문제가 뜬다
> 2. 맞히면 다음 사람에게 넘어간다
> 3. **아이**: 한 턴에 두 번 틀리면 터진다
> 4. **어른**: 판 전체에서 두 번 틀리면 터진다
> 5. 시간이 다 되면 들고 있는 사람이 터진다
>
> ⏱ 남은 시간은 안 보인다. 소리가 빨라지면 조심!
